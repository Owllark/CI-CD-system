apiVersion: v1
kind: Pod
metadata:
  namespace: jenkins
spec:
  containers:
  - name: frontend-test
    image: owllark/jenkins-agent-frontend-test:latest
    tty: true
  imagePullSecrets:
  - name: dockerhub-secret
